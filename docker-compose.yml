services:
  engage-victoria-mcp:
    build: .
    container_name: engage-victoria-mcp
    restart: unless-stopped
    environment:
      - KB_API_URL=https://ev-kb.doghouse.cloud
      - KB_SITE_NAME=Engage Victoria Knowledge
    stdin_open: true
    tty: true
